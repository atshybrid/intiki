import { SEO_PAGES } from "@/lib/seo";

export const metadata = SEO_PAGES.subscription;

export default function SubscriptionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
